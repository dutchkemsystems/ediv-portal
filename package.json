{
  "name": "educationdistrictivportal",
  "version": "1.0.0",
  "private": true,
  "description": "Education District IV Portal — root package for Render build (triggers Node.js install)",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
